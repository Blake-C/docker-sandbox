{
  "name": "docker-image",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "scripts": {
  	// "docker": "docker run --name docker-nginx -d -p 80:80 -v $PWD:/usr/share/nginx/html nginx"
    "docker": "docker-compose up -d" 
  }
}
